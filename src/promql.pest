digits = { ASCII_DIGIT+ }
duration_unit = { "ns" | "us" | "ms" | "s" | "m" | "h" | "d" | "w" | "y" }
duration_part = { digits ~ duration_unit }
duration = { duration_part+ | digits }

input = {SOI ~ EOI}
